#!/bin/bash

declare -a DEP_DEV=(
    "flutter"
    "android-studio"
    "nvim"
    "rust"
)

declare -a DEP_PERS=(
    "google-chrome-stable"
    "spotify"
)

declare -a DEP_OS=(
    # OS RELATED DEPENDENCIES    
)
